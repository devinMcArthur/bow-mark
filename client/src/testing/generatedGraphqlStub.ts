// Minimal stub for unit tests — exports only the enums used by calculator types.
// Values MUST match server/src/typescript/tenderPricingSheet.ts and client/src/generated/graphql.tsx.

export enum RateBuildupOutputKind {
  CrewHours = "CrewHours",
  Material = "Material",
}
