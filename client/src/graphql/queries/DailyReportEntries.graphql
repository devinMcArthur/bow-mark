query DailyReportEntries($dailyReportId: ID!) {
  dailyReportEntries(dailyReportId: $dailyReportId) {
    _id
    text
    documentIds
    isIssue
    createdAt
    updatedAt
    createdByUser {
      _id
      name
    }
  }
}
